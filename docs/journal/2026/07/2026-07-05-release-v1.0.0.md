# 2026-07-05 — Release v1.0.0 (roadmap 10.4, closes M10)

## What got done

- Prepared the **v1.0.0 release** — the final roadmap item. This closes **Milestone 10** and the
  whole pre-1.0 development line (Milestones 1–10). Per AGENTS §11 / `docs/workflow/release.md`,
  the agent prepares the release PR; the maintainer opens, merges, and publishes.
- **Version bump:** `D4NP_UTIL_VERSION_{MAJOR,MINOR,PATCH,STRING}` → `1.0.0` in `version.hpp`;
  updated the version-check tests (`smoke_test`, `library_version_test`) and the README status
  badge (`Status-v1.0.0`).
- **Changelog roll:** moved the entire `[Unreleased]` section into
  `docs/changelog/v1/v1.0.0.md` (merging the two `### Added` blocks into one), reset
  `[Unreleased]` to an empty scaffold, and added the released-versions index row.
- **Release notes:** drafted `docs/releases/v1.0.0.md` (highlights, usage, compatibility) and
  indexed it in `docs/releases/README.md`.
- **Roadmap/README:** flipped 10.4, marked README Milestone 10 ✅, and set Spec Coverage Map §1
  (Objective) to ✅ (the 1.0 release realizes the objective).

## Notes / decisions

- **v1.0.0 subsumes the pre-1.0 line.** Milestones 1–9 were developed but never individually
  published (the batched release was always "on offer"); rather than retroactively tag
  0.x versions, 1.0.0 is the first published release and consolidates the whole surface. Recorded
  in the release notes' Compatibility section. From here, SemVer governs (additions MINOR,
  breaks MAJOR).
- No ADR for the release itself — the process is `docs/workflow/release.md`; the substantive 1.0
  decisions already have ADRs (API freeze 0026, benchmarks 0027, coverage 0028).

## Verification

- `consistency_lint.py` passing — **version lockstep** now checks `version.hpp` (1.0.0) ==
  README badge (1.0.0) == latest `docs/changelog/v*/` (1.0.0) == latest `docs/releases/` (1.0.0).
- Full suite green with the bumped constants (the version tests assert 1.0.0). `clang-format`
  clean on the touched tests.
- CI note: Actions minutes still exhausted (billing), so the release PR's checks fail at startup,
  not on content. Verified locally.

## Remaining human steps (per docs/workflow/release.md)

1. Maintainer opens and merges the release PR (#…).
2. **Agent** (post-merge, if delegated) tags: `git tag -a v1.0.0 -m "egl-util-cpp v1.0.0"` and
   `git push origin v1.0.0` — the tag push lets CI draft the GitHub Release.
3. Maintainer clicks **Publish** on the drafted GitHub Release.
4. CI builds and attaches artifacts on the tag.

## Project state

- **Milestones 1–10 complete. v1.0.0 prepared.** The roadmap is fully checked off. Post-1.0 work
  is maintenance (`docs/workflow/maintenance.md`): fixes → PATCH, additive features → MINOR,
  breaking changes → MAJOR, each with its own ADR where the rationale is non-obvious.

## How the next session resumes

- If the release PR is merged: tag and push `v1.0.0` (agent carry-through), then hand the drafted
  GitHub Release to the maintainer to publish. Otherwise, address any review feedback on the
  release PR. There is no further roadmap item — future work starts from a new maintenance/feature
  item added to `ROADMAP.md`.
