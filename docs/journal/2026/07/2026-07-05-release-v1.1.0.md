# 2026-07-05 — Release v1.1.0 (Milestone 11)

## What got done

Cut the **v1.1.0** release consolidating Milestone 11 (Specification & Assurance Hardening) — the
seven-PR follow-up to the post-1.0 specification review.

- **Version bump** `1.0.0 → 1.1.0`: `D4NP_UTIL_VERSION_{MINOR,STRING}` in `version.hpp` + the README
  `Status-v1.1.0` badge (version-lockstep).
- **Changelog rolled:** `[Unreleased]` entries moved into `docs/changelog/v1/v1.1.0.md` (+ index row);
  `[Unreleased]` reset to empty sections.
- **Release notes** drafted at `docs/releases/v1.1.0.md` (+ index row).

## Why MINOR

M11 added user-visible surface (the `EGL_UTIL_BUILD_FUZZERS` option + `fuzz` preset, the threat
model, extended vectors) with **no breaking change** — no public symbol removed/renamed/changed.
Per the [maintenance decision tree](../../workflow/maintenance.md): new opt-in surface, everything
existing keeps working → **MINOR**. Fully drop-in from v1.0.0.

## Verification

- `python tools/consistency_lint.py` → OK (version-lockstep: `version.hpp` 1.1.0 == README badge ==
  latest `docs/changelog/v1/v1.1.0.md` == latest `docs/releases/v1.1.0.md`).
- No code logic change in this PR (version constant only); the M11 work was verified in its PRs.

## Project state

- **Milestones 1–11 complete.** Version **1.1.0**.

## Handoff (agent-vs-human boundary, AGENTS §11)

- The agent drafted this release PR (version bump, changelog roll, release notes). **The maintainer
  opens/merges it and publishes the GitHub Release**, then (if delegated) the annotated tag
  `v1.1.0` is pushed. **Close the "M11 — Specification & Assurance Hardening" GitHub milestone** when
  this PR merges.
- No further roadmap items are queued; the spec-review backlog is fully addressed.
